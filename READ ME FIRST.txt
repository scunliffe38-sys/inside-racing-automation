INSIDE RACING — the complete repository contents
Rebuilt 19 September 2026, with the cover framing rebuilt
================================================

This is everything the repository holds, not just the changed files: every
page, every parser, the fonts, the artwork, the sample inputs, the bake and
the notes. Use it when you want the clone brought wholly into line rather
than patched.

WHAT TO DO
----------
1. Unzip this folder somewhere outside your clone.
2. Open your clone in Explorer (GitHub Desktop: Ctrl+Shift+F).
3. Copy everything EXCEPT "READ ME FIRST.txt" into the clone, keeping the
   folder structure. Overwrite when asked.
4. GitHub Desktop's Changes tab will list only the files that actually
   differ — most of these are already identical to what is pushed.
5. Commit to main, then Push origin.

Nothing is deleted by copying, so a file that exists in the clone but not
here stays put. If you would rather start clean, delete the clone's contents
first (leaving the hidden .git folder alone) and then copy this in.

WHAT IS IN IT
-------------
Top level          the section pages and the assembled edition, the producer
                   console, the input checklist, index.html, support.js,
                   doc-page.js, image-slot.js
parsers/           the seventeen input parsers, pagination, advertising and
                   cover framing
assets/            masthead and heading artwork, house advertising, photos
fonts/             Gotham Narrow Book and Bold, Gotham Extra Narrow Book and
                   Bold
inputs/            a full set of sample inputs, September and October
data/edition.json  the baked edition the assembled pages read
docs/              architecture, run sheet, checks guide, blueprint,
                   testing and GitHub plan, input sources worksheet
CLAUDE.md          project notes, also as PROJECT-NOTES.md
github.md          the sync record
README.md          the repository front page
.nojekyll          keeps GitHub Pages from rewriting the folder names

CHANGED SINCE THE LAST PUSH
---------------------------
Every page, plus the font, the parsers, two new inputs and October's cover:

fonts/GothamNarrow-Bold.otf          NEW — the real bold face
  └ declared at weight 700 on all twenty pages, which is why every .dc.html
    and index.html appears in the changed list. The hairline stroke that
    stood in for bold is gone, and the chart's fit measure now reads a word
    at the weight it will print.
inputs/Feature Race Deadlines - Entry Deadlines.xlsx   NEW — the staged
inputs/Meeting Corrections - Entry Deadlines.xlsx      feature schedule and
  └ both hand-kept, both optional, October 2026 filled in. parsers/
    deadlines.js reads them in the same call as the export: the feature sheet
    replaces every feature line the export calculates, the corrections fix a
    venue name or a time on the ordinary lines.
parsers/deadlines.js                 the adjustment step
parsers/source.js                    the two inputs in the catalogue
parsers/cover-frame.js               the cover framing — the photograph starts
  └ below the masthead and fills the page from there; the hero is measured
    from its leading edge, sized to fill the clear depth (and further where
    the teaser strip demands it), centred in the clear space down the page,
    and placed across the page from column energy read over its own rows.
parsers/ads.js                       HR Assist benched; full pages bookable
Programs Summary Chart.dc.html       chart typography and colour
Entry Deadlines.dc.html              the ENTRIES / ACCEPTANCES / RIDERS labels
Permits and Riders Agents.dc.html    the trials riders' names
Race Series.dc.html                  packing, 15pt gap, highweight last
Inside Racing Edition.dc.html        all of the above, plus the contents rule
Inside Racing Edition-print.dc.html  and the Division of Races Policy
Inside Racing Part 1-3.dc.html
Producer Console.dc.html             the red-row fix, deadlines step summary
Jump Outs and Flat Trials.dc.html    trials title measured from the real depth
Notices.dc.html                      stewards' room spacing
assets/photos/cover.jpg              October's photograph
inputs/Cover.jpg                     the same at camera resolution
CLAUDE.md / PROJECT-NOTES.md         project notes
github.md                            sync record
data/edition.json                    September's bake

Suggested commit summary:
  "Gotham Narrow Bold, deadlines adjustment sheets, October review round two"

Suggested commit summary:
  "Cover framing for deep portraits, console conditional rows, layout fixes,
   baked edition"

NOTE ON data/edition.json
-------------------------
This copy is September's bake. Run the console against your October folder
and re-bake if October is what should be published.
